config:
  target: 'https://api-pub.bitfinex.com/v2'
  phases:
    - duration: 1
      arrivalRate: 1
  processor: "./auth-api-functions.js"
scenarios:
  - flow:
    - post:
        url: "/auth/r/wallets"
        beforeResponse: "setAuthHeaders"
        afterResponse: "wallets"

